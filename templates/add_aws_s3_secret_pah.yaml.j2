---
apiVersion: v1
kind: Secret
metadata:
  name: "{{ s3_bucket_secret_pah_name }}"
  namespace: "{{ aap_ocp_namespace }}"
type: Opaque
stringData:
  s3-access-key-id: "{{ aws_access_key }}"
  s3-secret-access-key: "{{ aws_secret_key }}"
  s3-bucket-name: "{{ s3_bucket_pah_name }}"
  s3-region: "{{ aws_region }}"
...
